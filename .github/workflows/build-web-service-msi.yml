name: Build Fortuna Faucet Web Service Installer

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  NODE_VERSION: '20'
  PYTHON_VERSION: '3.12'

jobs:
  build-frontend:
    name: '📦 Build Frontend'
    timeout-minutes: 15
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.7
      - name: Setup Node.js
        uses: actions/setup-node@v4.0.3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: 'web_platform/frontend/package-lock.json'
      - name: Frontend - Install & Build
        shell: pwsh
        run: |
          cd web_platform/frontend
          npm ci
          npm audit --audit-level=moderate # Added security check
          npm run build
      - name: Upload Frontend Artifact
        uses: actions/upload-artifact@v4.3.4
        with:
          name: frontend-build-output-${{ github.sha }}
          path: web_platform/frontend/out
          retention-days: 1

  build-backend:
    name: '🐍 Build Backend'
    timeout-minutes: 20
    needs: [build-frontend]
    runs-on: windows-latest
    env:
      PYTHONUTF8: "1"
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.7
      - name: Setup Python
        uses: actions/setup-python@v5.1.1
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
          cache-dependency-path: 'python_service/requirements.txt'
      - name: Download Frontend Artifact
        uses: actions/download-artifact@v4.1.8
        with:
          name: frontend-build-output-${{ github.sha }}
          path: ./temp-frontend
      - name: '✅ [FIX] Stage Frontend Artifact for PyInstaller'
        shell: pwsh
        run: |
          $sourceDir = "./temp-frontend"
          $destinationDir = "web_platform/frontend/out"
          if (Test-Path $destinationDir) {
            Remove-Item -Recurse -Force $destinationDir
          }
          New-Item -ItemType Directory -Path $destinationDir -Force | Out-Null
          Copy-Item -Path "$sourceDir/*" -Destination $destinationDir -Recurse -Force
          Write-Host "✅ Frontend artifact staged from $sourceDir to $destinationDir."
          Write-Host "--- Verifying staged content: ---"
          Get-ChildItem -Path $destinationDir -Recurse | ForEach-Object { Write-Host $_.FullName }
          Write-Host "--- Verification complete. ---"
      - name: '✅ [MONITOR] Ensure Required Directories Exist'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path "python_service/adapters" -Force | Out-Null
          New-Item -ItemType Directory -Path "python_service/data" -Force | Out-Null
          New-Item -ItemType Directory -Path "python_service/json" -Force | Out-Null
          Write-Host "✅ Ensured required data, json, and adapters directories exist."
      - name: '✅ [MONITOR] Verify Critical Files'
        shell: pwsh
        run: |
          if (-not (Test-Path "python_service/main.py")) { throw "❌ FATAL: python_service/main.py not found" }
          Write-Host "✅ Critical files verified."
      - name: Backend - Install Dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          pip install -r python_service/requirements-dev.txt
      - name: Backend - Security Audit
        shell: pwsh
        run: |
          pip-audit -r python_service/requirements.txt --local
      - name: Backend - Build with PyInstaller
        shell: pwsh
        run: |
          pyinstaller fortuna-backend-webservice.spec --noconfirm
      - name: Upload Backend Artifact
        uses: actions/upload-artifact@v4.3.4
        with:
          name: backend-executable-${{ github.sha }}
          path: dist/fortuna-backend.exe
          retention-days: 1
          if-no-files-found: error

  smoke-test-backend:
    name: '🧪 Smoke Test Backend Executable'
    timeout-minutes: 10
    needs: [build-backend]
    runs-on: windows-latest
    steps:
      - name: Download Backend Executable
        uses: actions/download-artifact@v4.1.8
        with:
          name: backend-executable-${{ github.sha }}
      - name: Run Smoke Test
        shell: pwsh
        env:
          API_KEY: "a_secure_test_api_key_that_is_long_enough_for_smoke_test"
        run: |
          $exe = "./fortuna-backend.exe"
          $logFileBase = "backend-log"
          $stdOutPath = "$logFileBase-out.txt"
          $stdErrPath = "$logFileBase-err.txt"
          $process = Start-Process -FilePath $exe -PassThru -NoNewWindow -RedirectStandardOutput $stdOutPath -RedirectStandardError $stdErrPath
          $serverReady = $false
          Write-Host "--- Starting Smoke Test: Polling /health and /api/races endpoints for 60s ---"
          try {
            foreach ($i in 1..30) {
              if ($process.HasExited) {
                throw "[FATAL] Backend process crashed during smoke test. Exit Code: $($process.ExitCode)"
              }
              try {
                $response = Invoke-WebRequest -Uri "http://127.0.0.1:8000/health" -TimeoutSec 1 -UseBasicParsing
                if ($response.StatusCode -eq 200) {
                  $headers = @{ "X-API-Key" = $env:API_KEY }
                  $apiResponse = Invoke-WebRequest -Uri "http://127.0.0.1:8000/api/races" -Headers $headers -TimeoutSec 1 -UseBasicParsing -ErrorAction SilentlyContinue
                  if ($apiResponse.StatusCode -eq 200) {
                    Write-Host "[OK] Health check and API test passed on attempt $i!"
                    $serverReady = $true
                    break
                  }
                }
              } catch {
                Write-Host "Attempt $i... server not ready."
              }
              Start-Sleep -Seconds 2
            }
          } finally {
            Stop-Process -Id $process.Id -Force -ErrorAction SilentlyContinue
            if (-not $serverReady) {
              Write-Host "--- ❌ TEST FAILED ---"
              if (Test-Path $stdOutPath) {
                Write-Host "--- STDOUT ---"
                Get-Content $stdOutPath | Write-Host
              }
              if (Test-Path $stdErrPath) {
                Write-Host "--- STDERR ---"
                Get-Content $stdErrPath | Write-Host
              }
              throw "[FATAL] Backend smoke test failed. Server never became healthy."
            }
          }
          Write-Host "✅ Backend smoke test passed."


  build-wix-installer:
    name: '🔥 Build WiX Installer (Modernized)'
    timeout-minutes: 15
    needs: [build-backend, build-frontend]
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4.1.7

      - name: Download Build Artifacts
        uses: actions/download-artifact@v4.1.8
        with:
          path: ./artifacts

      - name: Setup .NET for WiX v4
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Install WiX v4 Toolset
        shell: pwsh
        run: |
          dotnet tool install --global wix

      - name: Add WiX Util and Firewall Extensions
        shell: pwsh
        run: |
          wix extension add WixToolset.Util.wixext
          wix extension add WixToolset.Firewall.wixext

      - name: Stage Files for WiX
        shell: pwsh
        run: |
          $wixStagingDir = "build_wix/staging"
          $uiStagingDir = Join-Path $wixStagingDir "ui"

          if (Test-Path $wixStagingDir) { Remove-Item -Recurse -Force $wixStagingDir }
          New-Item -ItemType Directory -Path $uiStagingDir | Out-Null

          Copy-Item -Path "./artifacts/backend-executable-${{ github.sha }}/fortuna-backend.exe" -Destination (Join-Path $wixStagingDir "fortuna-backend.exe")
          Copy-Item -Path "./artifacts/frontend-build-output-${{ github.sha }}/*" -Destination $uiStagingDir -Recurse
          Write-Host "✅ All assets staged for WiX build."

      - name: Generate Frontend.wxs (WiX v4 Heat)
        shell: pwsh
        run: |
          dotnet msbuild build_wix/harvest.wixproj -restore -t:GenerateFrontendWxs
          Write-Host "✅ UI directory harvested into frontend_components.wxs via MSBuild."

      - name: Validate frontend_components.wxs Was Generated
        shell: pwsh
        run: |
          $filePath = "build_wix/frontend_components.wxs"
          if (-not (Test-Path $filePath)) {
            throw "❌ frontend_components.wxs was not generated by the MSBuild harvest command."
          }
          $content = Get-Content $filePath -Raw
          if ($content -notmatch '<ComponentGroup Id="FrontendComponents">') {
            throw "❌ frontend_components.wxs does not contain the expected ComponentGroup 'FrontendComponents'."
          }
          $fileCount = ([regex]::Matches($content, '<File')).Count
          if ($fileCount -eq 0) {
            throw "❌ FATAL: frontend_components.wxs was generated but contains NO files. Check the staging directory and harvest.wixproj configuration."
          }
          Write-Host "✅ frontend_components.wxs is valid and contains $fileCount files."

      - name: Build MSI with WiX
        shell: pwsh
        run: |
          $sourceDir = "$PWD/build_wix/staging"
          $uiDir = "$sourceDir/ui"

          # Build the MSI
          wix build `
            build_wix/Product_WithService.wxs `
            build_wix/frontend_components.wxs `
            -ext WixToolset.Util.wixext `
            -ext WixToolset.Firewall.wixext `
            -d "SourceDir=$sourceDir" `
            -d "FrontendSourceDir=$uiDir" `
            -o dist/Fortuna-Full-App-Service-v4.msi

          Write-Host "✅ MSI created successfully"

      - name: Upload WiX MSI Artifact
        uses: actions/upload-artifact@v4.3.4
        with:
          name: fortuna-wix-installer-windows-v4
          path: dist/Fortuna-Full-App-Service-v4.msi
          retention-days: 7
