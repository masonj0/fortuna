name: 🚀 Supreme Combo Build (MSI)

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-msi-reusable-core.yml
    secrets: inherit
