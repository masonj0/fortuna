name: 🧟 🏛️ Fortuna Web Service MSI (The Veteran Reborn)

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-msi-reusable-core-v2.yml
    secrets: inherit
