version: '3.8'

services:
  fortuna:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./json:/app/json
      - ./logs:/app/logs
    environment:
      - FORTUNA_MODE=docker
      - FORTUNA_PORT=8000
    restart: unless-stopped
